/*
Debemos lograr tomar un nombre con 'prompt' 
y luego mostrarlo por 'alert' al presionar el botón  'mostrar'*/
function mostrar()
{
	let name; // declaro variable (siempre usar LET)

	//name = "juan"; // inicializo var

	let nombre = "roberto"; // declaro e inicializo var 

	name = prompt("Ingrese su nombre");
	
	alert(name);
}

